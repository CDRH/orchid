<% @site_section = "search" %>
<% if params["q"].blank? && params["f"].blank? %>
  <h1><%= t "search.all_items", default: "All Items" %></h1>
<% else %>
  <h1><%= t "search.search_results", default: "Search Results" %></h1>
<% end %>

<div class="row search_form">
  <%= render "search_box" %>

  <div class="col-sm-3">
    <div class="clearfix">
      <h2 class="pull-left">
        <%= t "search.filters.filter_by", default: "Filter by" %>:
      </h2>
      <%= render "clear_facets" %>
    </div>
    <%= render "date_limit" %>
    <%= render "facets", facets: @res.facets %>
  </div> <!-- col-md-3 -->

  <div class="col-sm-9">
    <%= render "summary_boxes" %>
    <%= render "sort_and_page_ui" %>

    <!-- search results section -->
    <%= render "search_res_count" %>
    <%= render "search_res_items" %>

    <%= render "sort_and_page_ui" %>
  </div>
</div>
