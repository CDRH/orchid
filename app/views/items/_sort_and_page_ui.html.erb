<div class="row search_controls">
  <div class="col-md-4 search_limit">
    <% if (@res.count > 1) %>
      <%= sorter %>
    <% end %>
  </div>
  <div class="col-md-8 pagination_container">
    <%= paginator @res.pages %>
  </div>
</div>
